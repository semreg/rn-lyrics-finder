export enum Paths {
  DASHBOARD = 'dashboard',
  HOME = 'home',
  TRACK_LIST = 'track_list',
  TRACK_DETAILS = 'track_details',
  FAVORITES = 'favorites'
}

export enum Stacks {
  HOME_STACK = 'home_stack',
  BOTTOM_TAB_STACK = 'bottom_tab_stack',
  BROWSE_TRACKS_STACK = 'browse_tracks_stack',
  FAVORITES_STACK = 'favorites_stack'
}

export enum Screens {
  BROWSE_TRACKS_LIST = 'browse-tracks/list',
  BROWSE_TRACKS_DETAILS = 'browse-tracks/details',
  FAVORITES = 'favorites',
  FAVORITES_TRACK_DETAILS = 'favorites/details'
}
